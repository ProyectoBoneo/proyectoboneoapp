import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    perfilAcademicoItemView: {
        flex: 1,
        alignItems: 'flex-start',
        justifyContent: 'space-between',
        flexDirection: 'row',
        height: 50
    },
    perfilAcademicoTextNoLeido: {
        fontWeight: 'bold',
    },
});
